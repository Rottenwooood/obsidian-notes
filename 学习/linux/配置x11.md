Ubuntu连接Ubuntu
```
sudo apt update
sudo apt install xauth

sudo nano /etc/ssh/sshd_config
## 确认以下
X11Forwarding yes
X11UseLocalhost yes

sudo systemctl restart ssh
```

```
ssh -X username@server_ip
# 或者
ssh -Y username@server_ip
# test
xclock
```

```
# 从~/.ssh/config修改
Host 10.95.80.227
  HostName 10.95.80.227
  ForwardX11 yes
  ForwardX11Trusted yes
  User yukun
  Port 24
```